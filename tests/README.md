# tests

- `unit/` — host logic (sampler, scheduler, cache) on CPU
- `conformance/` — wire protocol vs docs/halogen/WIRE-PROTOCOL.md
- `equivalence/` — A/B greedy parity vs the original engine on a Strix Halo box
