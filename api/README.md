# chlorine-server API

OpenAI-compatible front-end (clean-room reimplementation of the halogen
serving layer): BPE tokenizer, Qwen chat template, /v1/completions,
/v1/chat/completions, SSE, tool calls. Speaks the token protocol to the
engine (docs/halogen/WIRE-PROTOCOL.md).
